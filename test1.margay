getPower = fn(n) {
    
    return fn(x) {
        p = 1;
        i = 0;
        
        for (i < n) {
            p = p * x;
            i = i + 1;
        }

        return p;
    }
}

powTwo = getPower(2);
powThree = getPower(3);

b = powTwo(5);
a = powThree(2);

write(b + a * a);