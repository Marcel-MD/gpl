getPower = fn(n) {
    
    return fn(x) {
        p = 1;
        i = 0;
        
        for (i < n) {
            p = p * x;
            i = i + 1;
        }

        return p;
    }
}

firstPow = getPower(2);
secondPow = getPower(3);

b = firstPow(5);
a = secondPow(2);

return b + a * a;