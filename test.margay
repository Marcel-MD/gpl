pow = fn(x, n) {
    p = 1;
    i = 0;
    
    for (i < n) {
        p = p * x;
        i = i + 1;
    }

    return p;
}

p = pow(2, 3);



add = fn(x) {
    fn(y) { x + y }
}

addTwo = add(2);
s = addTwo(3);

return s;